<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>Pastor Portal - {{ config('app.name') }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            font-family: 'Inter', sans-serif;
        }
        .sidebar {
            background: linear-gradient(180deg, #1e40af 0%, #1e3a8a 100%);
        }
        .nav-item {
            transition: all 0.3s ease;
        }
        .nav-item:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }
        .nav-item.active {
            background: rgba(255, 255, 255, 0.2);
            border-left: 4px solid #fbbf24;
        }
    </style>
</head>
<body class="min-h-screen">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar Navigation -->
        <aside class="sidebar w-64 text-white flex-shrink-0 overflow-y-auto">
            <div class="p-6">
                <div class="flex items-center space-x-3 mb-8">
                    <div class="w-12 h-12 bg-yellow-500 rounded-full flex items-center justify-center">
                        <i class="fas fa-church text-blue-900 text-xl"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold">Pastor Portal</h1>
                        <p class="text-xs text-blue-200">Ministry Dashboard</p>
                    </div>
                </div>

                <nav class="space-y-2">
                    <a href="{{ route('pastor.dashboard') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.dashboard') ? 'active' : '' }}">
                        <i class="fas fa-home w-5"></i>
                        <span>Dashboard</span>
                    </a>

                    <a href="{{ route('pastor.sermons') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.sermons') ? 'active' : '' }}">
                        <i class="fas fa-book-open w-5"></i>
                        <span>Sermons / Messages</span>
                    </a>

                    <a href="{{ route('pastor.prayer-requests') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.prayer-requests') ? 'active' : '' }}">
                        <i class="fas fa-praying-hands w-5"></i>
                        <span>Prayer Requests</span>
                    </a>

                    <a href="{{ route('pastor.members') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.members') ? 'active' : '' }}">
                        <i class="fas fa-users w-5"></i>
                        <span>Members</span>
                    </a>

                    <a href="{{ route('pastor.events') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.events') ? 'active' : '' }}">
                        <i class="fas fa-calendar-alt w-5"></i>
                        <span>Programs & Events</span>
                    </a>

                    <a href="{{ route('pastor.counselling') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.counselling') ? 'active' : '' }}">
                        <i class="fas fa-comments w-5"></i>
                        <span>Counselling Sessions</span>
                    </a>

                    <a href="{{ route('pastor.finance') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.finance') ? 'active' : '' }}">
                        <i class="fas fa-hand-holding-usd w-5"></i>
                        <span>Finance Overview</span>
                    </a>

                    <a href="{{ route('pastor.broadcast') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.broadcast') ? 'active' : '' }}">
                        <i class="fas fa-bullhorn w-5"></i>
                        <span>Announcements</span>
                    </a>

                    <a href="{{ route('pastor.ai-assistant') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.ai-assistant') ? 'active' : '' }}">
                        <i class="fas fa-robot w-5"></i>
                        <span>AI Ministry Assistant</span>
                    </a>

                    <a href="{{ route('pastor.settings') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('pastor.settings') ? 'active' : '' }}">
                        <i class="fas fa-cog w-5"></i>
                        <span>Settings</span>
                    </a>

                    <div class="border-t border-white/20 my-4"></div>

                    <form action="{{ route('logout') }}" method="POST">
                        @csrf
                        <button type="submit" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg w-full text-left hover:bg-red-600">
                            <i class="fas fa-sign-out-alt w-5"></i>
                            <span>Logout</span>
                        </button>
                    </form>
                </nav>
            </div>
        </aside>

        <!-- Main Content Area -->
        <main class="flex-1 overflow-y-auto bg-gray-50 p-8">
            @if(session('success'))
                <div class="mb-6 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
                    <i class="fas fa-check-circle mr-2"></i>
                    {{ session('success') }}
                </div>
            @endif

            @if(session('error'))
                <div class="mb-6 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg">
                    <i class="fas fa-exclamation-circle mr-2"></i>
                    {{ session('error') }}
                </div>
            @endif

            @yield('content')
        </main>
    </div>

    @stack('scripts')
</body>
</html>
