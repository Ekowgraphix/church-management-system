<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>{{ config('app.name') }} - Member Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Inter', sans-serif;
        }
        .glass-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .sidebar {
            background: linear-gradient(180deg, #1a237e 0%, #283593 100%);
        }
        .nav-item {
            transition: all 0.3s ease;
        }
        .nav-item:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }
        .nav-item.active {
            background: rgba(255, 255, 255, 0.2);
            border-left: 4px solid #4CAF50;
        }
    </style>
</head>
<body class="min-h-screen">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar Navigation -->
        <aside class="sidebar w-64 text-white flex-shrink-0 overflow-y-auto">
            <div class="p-6">
                <div class="flex items-center space-x-3 mb-8">
                    <div class="w-12 h-12 bg-white rounded-full flex items-center justify-center">
                        <i class="fas fa-church text-indigo-600 text-xl"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold">Church Portal</h1>
                        <p class="text-xs text-gray-300">Member Area</p>
                    </div>
                </div>

                <nav class="space-y-2">
                    <a href="{{ route('portal.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('portal.index') ? 'active' : '' }}">
                        <i class="fas fa-home w-5"></i>
                        <span>Dashboard</span>
                    </a>

                    <a href="{{ route('portal.profile') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('portal.profile') ? 'active' : '' }}">
                        <i class="fas fa-user w-5"></i>
                        <span>Profile Settings</span>
                    </a>

                    <a href="{{ route('notifications.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-bell w-5"></i>
                        <span>Notifications</span>
                        @if(auth()->user()->unreadNotifications->count() > 0)
                            <span class="ml-auto bg-red-500 text-white text-xs rounded-full px-2 py-1">
                                {{ auth()->user()->unreadNotifications->count() }}
                            </span>
                        @endif
                    </a>

                    <a href="{{ route('events.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-calendar-alt w-5"></i>
                        <span>Event Calendar</span>
                    </a>

                    <a href="{{ route('portal.giving') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('portal.giving') ? 'active' : '' }}">
                        <i class="fas fa-hand-holding-usd w-5"></i>
                        <span>Offering & Tithe</span>
                    </a>

                    <a href="{{ route('ai.chat') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-robot w-5"></i>
                        <span>AI Chat Assistant</span>
                    </a>

                    <a href="{{ route('prayer-requests.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-praying-hands w-5"></i>
                        <span>Prayer Requests</span>
                    </a>

                    <a href="{{ route('devotional.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-book-open w-5"></i>
                        <span>Daily Devotional</span>
                    </a>

                    <a href="{{ route('small-groups.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-users w-5"></i>
                        <span>Join a Ministry</span>
                    </a>

                    <a href="{{ route('chat.index') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg">
                        <i class="fas fa-comments w-5"></i>
                        <span>Member Chatroom</span>
                    </a>

                    <div class="border-t border-white/20 my-4"></div>

                    <form action="{{ route('logout') }}" method="POST">
                        @csrf
                        <button type="submit" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg w-full text-left hover:bg-red-600">
                            <i class="fas fa-sign-out-alt w-5"></i>
                            <span>Logout</span>
                        </button>
                    </form>
                </nav>
            </div>
        </aside>

        <!-- Main Content Area -->
        <main class="flex-1 overflow-y-auto bg-gray-50 p-8">
            @if(session('success'))
                <div class="mb-6 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
                    <i class="fas fa-check-circle mr-2"></i>
                    {{ session('success') }}
                </div>
            @endif

            @if(session('error'))
                <div class="mb-6 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg">
                    <i class="fas fa-exclamation-circle mr-2"></i>
                    {{ session('error') }}
                </div>
            @endif

            @yield('content')
        </main>
    </div>

    @stack('scripts')
</body>
</html>
