<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@yield('title', 'Ministry Leader Portal') - Church Management</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar -->
        <aside class="w-64 bg-gradient-to-b from-purple-800 to-purple-900 text-white flex-shrink-0 hidden md:block">
            <div class="p-6">
                <h2 class="text-2xl font-bold">Ministry Leader</h2>
                <p class="text-purple-200 text-sm">{{ auth()->user()->name }}</p>
            </div>
            
            <nav class="mt-6">
                <a href="{{ route('ministry-leader.dashboard') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.dashboard') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-home mr-3"></i>
                    <span>Dashboard</span>
                </a>
                
                <a href="{{ route('ministry-leader.members') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.members') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-users mr-3"></i>
                    <span>Members</span>
                </a>
                
                <a href="{{ route('ministry-leader.groups') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.groups') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-user-friends mr-3"></i>
                    <span>Small Groups</span>
                </a>
                
                <a href="{{ route('ministry-leader.events') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.events') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-calendar-alt mr-3"></i>
                    <span>Events</span>
                </a>
                
                <a href="{{ route('ministry-leader.prayer-requests') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.prayer-requests') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-praying-hands mr-3"></i>
                    <span>Prayer Requests</span>
                </a>
                
                <a href="{{ route('ministry-leader.reports') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.reports') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-chart-line mr-3"></i>
                    <span>Reports</span>
                </a>
                
                <a href="{{ route('ministry-leader.communication') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.communication') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-comments mr-3"></i>
                    <span>Communication</span>
                </a>
                
                <a href="{{ route('ministry-leader.ai-assistant') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.ai-assistant') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-robot mr-3"></i>
                    <span>AI Assistant</span>
                </a>
                
                <a href="{{ route('ministry-leader.settings') }}" class="flex items-center px-6 py-3 hover:bg-purple-700 transition {{ request()->routeIs('ministry-leader.settings') ? 'bg-purple-700 border-l-4 border-white' : '' }}">
                    <i class="fas fa-cog mr-3"></i>
                    <span>Settings</span>
                </a>
                
                <form action="{{ route('logout') }}" method="POST" class="mt-4">
                    @csrf
                    <button type="submit" class="flex items-center px-6 py-3 hover:bg-purple-700 transition w-full text-left">
                        <i class="fas fa-sign-out-alt mr-3"></i>
                        <span>Logout</span>
                    </button>
                </form>
            </nav>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">
            <!-- Top Bar -->
            <header class="bg-white shadow-md py-4 px-6 flex justify-between items-center">
                <h1 class="text-2xl font-bold text-gray-800">@yield('header', 'Ministry Leader Portal')</h1>
                <div class="flex items-center space-x-4">
                    <div class="text-right">
                        <p class="text-sm font-semibold text-gray-700">{{ auth()->user()->name }}</p>
                        <p class="text-xs text-gray-500">Ministry Leader</p>
                    </div>
                    <img src="https://ui-avatars.com/api/?name={{ urlencode(auth()->user()->name) }}&background=9333ea&color=fff" 
                         alt="Profile" 
                         class="w-10 h-10 rounded-full">
                </div>
            </header>

            <!-- Content Area -->
            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-100 p-6">
                @if(session('success'))
                    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
                        {{ session('success') }}
                    </div>
                @endif
                
                @if(session('error'))
                    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
                        {{ session('error') }}
                    </div>
                @endif

                @yield('content')
            </main>
        </div>
    </div>
</body>
</html>
