<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>Volunteer Portal - {{ config('app.name') }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #f59e0b 0%, #fb923c 100%);
            font-family: 'Inter', sans-serif;
        }
        .sidebar {
            background: linear-gradient(180deg, #ea580c 0%, #f59e0b 100%);
        }
        .nav-item {
            transition: all 0.3s ease;
        }
        .nav-item:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }
        .nav-item.active {
            background: rgba(255, 255, 255, 0.2);
            border-left: 4px solid #10b981;
        }
    </style>
</head>
<body class="min-h-screen">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar Navigation -->
        <aside class="sidebar w-64 text-white flex-shrink-0 overflow-y-auto">
            <div class="p-6">
                <div class="flex items-center space-x-3 mb-8">
                    <div class="w-12 h-12 bg-green-500 rounded-full flex items-center justify-center">
                        <i class="fas fa-hands-helping text-orange-900 text-xl"></i>
                    </div>
                    <div>
                        <h1 class="text-xl font-bold">Volunteer</h1>
                        <p class="text-xs text-orange-200">Serving Portal</p>
                    </div>
                </div>

                <nav class="space-y-2">
                    <a href="{{ route('volunteer.dashboard') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.dashboard') ? 'active' : '' }}">
                        <i class="fas fa-home w-5"></i>
                        <span>Dashboard</span>
                    </a>

                    <a href="{{ route('volunteer.events') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.events') ? 'active' : '' }}">
                        <i class="fas fa-calendar-alt w-5"></i>
                        <span>Assigned Events</span>
                    </a>

                    <a href="{{ route('volunteer.tasks') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.tasks') ? 'active' : '' }}">
                        <i class="fas fa-tasks w-5"></i>
                        <span>Task Manager</span>
                    </a>

                    <a href="{{ route('volunteer.team') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.team') ? 'active' : '' }}">
                        <i class="fas fa-users w-5"></i>
                        <span>My Team / Group</span>
                    </a>

                    <a href="{{ route('volunteer.prayer') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.prayer') ? 'active' : '' }}">
                        <i class="fas fa-praying-hands w-5"></i>
                        <span>Prayer Requests</span>
                    </a>

                    <a href="{{ route('volunteer.ai-helper') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.ai-helper') ? 'active' : '' }}">
                        <i class="fas fa-robot w-5"></i>
                        <span>AI Helper</span>
                    </a>

                    <a href="{{ route('volunteer.communication') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.communication') ? 'active' : '' }}">
                        <i class="fas fa-comments w-5"></i>
                        <span>Communication</span>
                    </a>

                    <a href="{{ route('volunteer.settings') }}" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg {{ request()->routeIs('volunteer.settings') ? 'active' : '' }}">
                        <i class="fas fa-cog w-5"></i>
                        <span>Settings</span>
                    </a>

                    <div class="border-t border-white/20 my-4"></div>

                    <form action="{{ route('logout') }}" method="POST">
                        @csrf
                        <button type="submit" class="nav-item flex items-center space-x-3 px-4 py-3 rounded-lg w-full text-left hover:bg-red-600">
                            <i class="fas fa-sign-out-alt w-5"></i>
                            <span>Logout</span>
                        </button>
                    </form>
                </nav>
            </div>
        </aside>

        <!-- Main Content Area -->
        <main class="flex-1 overflow-y-auto bg-gray-50 p-8">
            @if(session('success'))
                <div class="mb-6 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
                    <i class="fas fa-check-circle mr-2"></i>
                    {{ session('success') }}
                </div>
            @endif

            @if(session('error'))
                <div class="mb-6 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg">
                    <i class="fas fa-exclamation-circle mr-2"></i>
                    {{ session('error') }}
                </div>
            @endif

            @yield('content')
        </main>
    </div>

    @stack('scripts')
</body>
</html>
